## Input file for automake to generate the Makefile.in used by configure

if CREATE_CNI_LIBRARIES
  CNIDIR = cni
endif

if CREATE_JNI_LIBRARIES
  JNIDIR = jni
endif

SUBDIRS = fdlibm $(CNIDIR) $(JNIDIR) target
DIST_SUBDIRS = fdlibm cni jni target

